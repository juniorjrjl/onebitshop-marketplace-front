import React from "react"
import { Container, ErrorTitle } from "./styled"

const ErrorScreen = () =>{
    return(
        <Container>
            <ErrorTitle>Alguma coisa não funcionou como o esperado</ErrorTitle>
        </Container>
    )
}

export default ErrorScreen